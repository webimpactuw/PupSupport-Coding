export default function DropdownCard(props) {

    return(
        <div>
            <h1>{props.heading}</h1>
            
        </div>
    );
}

function createCards(props) {
    for(let i = 0; i < props.numCards; i++) {
        
    }
}